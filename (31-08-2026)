SELECT
    VERSION() AS mysql_version,
    CURRENT_USER() AS connected_user,
    DATABASE() AS selected_database,
    @@autocommit AS autocommit_enabled;
show databases;
use nishazareentaj;
show tables;
select * from algonex_users;
select connection_id();
select current_user();
select upper(full_name) from algonex_users;
-- write a query to find the length of string mysql
select length("mysql");
-- write a query to remove leading and trailling spacesfrom "   hello mysql   '
select trim("   mysql   ");
-- write a query to extract the first 5 chaeacters from " database"
select substring("database",1,5);
-- write a query to concatinate "algonexa' and a'crew'
select concat("algonex","crew");
-- write a query to replace "java" with "python" in a string
select replace("java developer","java","python");
-- write a queryto display an employees"s first name aaaaand last name as a column
-- write a query to dislay the first 3 characters of every employees name
SELECT CONCAT(first_name, ' ', last_name) AS full_name
FROM algonex_users;
SELECT SUBSTRING(first_name, 1, 3) AS short_name
FROM algonex_users;
-- write a query to round 125.6789 to 2 decimaal places
select round("125.6789");
-- write a query to feind the ceiling value of 45.23
select ceil("45.23");
-- 11.write a query to find the floor value of 45.89
select floor("45.89");
-- write a query to find absolute value of -250
select abs("-250");
-- write a query to find reminder when 25 is divided by 4
select power(24,4);
select mod(24,4);
-- query to caluclate 5**2
select power(5,2);





SELECT
    VERSION() AS mysql_version,
    CURRENT_USER() AS connected_user,
    DATABASE() AS selected_database,
    @@autocommit AS autocommit_enabled;
show databases;
use nishazareentaj;
show tables;
select * from algonex_users;
select connection_id();
select current_user();
select upper(full_name) from algonex_users;
-- write a query to find the length of string mysql
select length("mysql");
-- write a query to remove leading and trailling spacesfrom "   hello mysql   '
select trim("   mysql   ");
-- write a query to extract the first 5 chaeacters from " database"
select substring("database",1,5);
-- write a query to concatinate "algonexa' and a'crew'
select concat("algonex","crew");
-- write a query to replace "java" with "python" in a string
select replace("java developer","java","python");
-- write a queryto display an employees"s first name aaaaand last name as a column
-- write a query to dislay the first 3 characters of every employees name
SELECT CONCAT(first_name, ' ', last_name) AS full_name
FROM algonex_users;
SELECT SUBSTRING(first_name, 1, 3) AS short_name
FROM algonex_users;
-- write a query to round 125.6789 to 2 decimaal places
select round("125.6789");
-- write a query to feind the ceiling value of 45.23
select ceil("45.23");
-- 11.write a query to find the floor value of 45.89
select floor("45.89");
-- write a query to find absolute value of -250
select abs("-250");
-- write a query to find reminder when 25 is divided by 4
select power(24,4);
select mod(24,4);
-- query to caluclate 5**2
select power(5,2);
select now();
select curdate();
select datediff("2026-08-20","2026-08-26");
-- the difference is now provides exact date and time of now 
-- but curdate only provides the exact dateof today
-- where as date diff provides the difference of days between two dates 
-- -- 16 SELECT 
--     EmployeeID, 
--     YEAR(JoiningDate) AS JoiningYear
-- FROM Employees;
/* SELECT 
    EmployeeID, 
    DATEDIFF(YEAR, JoiningDate, GETDATE()) AS YearsWorked
FROM Employees;
SELECT 
    EmployeeID, 
    FirstName, 
    LastName, 
    JoiningDate
FROM Employees
WHERE JoiningDate >= DATEADD(MONTH, -6, GETDATE());
SELECT 
    EmployeeID, 
    CONVERT(VARCHAR(10), JoiningDate, 105) AS FormattedDate
FROM Employees;
SELECT 
    EmployeeID, 
    Salary,
    CASE 
        WHEN Salary > 80000 THEN 'High'
        WHEN Salary BETWEEN 50000 AND 80000 THEN 'Medium'
        ELSE 'Low'
    END AS SalaryCategory
FROM Employees;
SELECT DISTINCT Salary
FROM Employees e1
WHERE 3 = (
    SELECT COUNT(DISTINCT Salary)
    FROM Employees e2
    WHERE e2.Salary > e1.Salary
);
SELECT EmployeeID, FirstName, LastName, Salary
FROM Employees
WHERE Salary > (SELECT AVG(Salary) FROM Employees);
SELECT 
    DepartmentID,
    COUNT(*) AS EmployeeCount,
    AVG(Salary) AS AverageSalary,
    MIN(Salary) AS MinimumSalary,
    MAX(Salary) AS MaximumSalary,
    SUM(Salary) AS TotalSalary
FROM Employees
GROUP BY DepartmentID;
SELECT 
    EmployeeID,
    FirstName,
    LastName,
    Salary,
    Salary * 1.10 AS IncrementedSalary
FROM Employees;
SELECT 
    EmployeeID,
    CONCAT(FirstName, ' ', LastName) AS FullName,
    LEN(CONCAT(FirstName, ' ', LastName)) AS NameLength
FROM Employees;
*/





--




