╰┈➤ MySQL Core Concepts( Data Types + Constraints + Keys + SQL Commands)
𝗥𝘂𝗹𝗲: No notes, no Google, no ChatGPT

Topics covered

↳ ᴅᴀᴛᴀ ᴛʏᴘᴇꜱ: Integer types, DECIMAL, FLOAT, DOUBLE, BIT, CHAR, VARCHAR, TEXT, BLOB, BINARY, VARBINARY, ENUM, SET, DATE, TIME, DATETIME, TIMESTAMP, YEAR, JSON
↳̲ ̲C̲o̲n̲s̲t̲r̲a̲i̲n̲t̲s̲:̲ PRIMARY KEY, FOREIGN KEY, UNIQUE, NOT NULL, DEFAULT, CHECK
Keys: Primary, foreign, candidate, alternate, super, composite, unique
Commands: CREATE, ALTER, DROP, TRUNCATE, RENAME, INSERT, UPDATE, DELETE, SELECT, USE, SHOW, DESCRIBE, GRANT, REVOKE, COMMIT, ROLLBACK, SAVEPOINT

SECTION A — MCQs
1. You are storing a customer's age
Which is the most appropriate choice?
A. VARCHAR(3)
B. INT
C. TEXT
D. DECIMAL(10,2) -- B

2. A grocery product price is: 149.99
Which data type is generally appropriate for exact monetary values?
A. FLOAT
B. DOUBLE
C. DECIMAL(10,2)
D. VARCHAR(10) -- C

3. Which is an approximate numeric type?
A. INT
B. DECIMAL
C. DOUBLE
D. BIGINT  -- C

4. Which type is designed for variable-length character strings?
A. CHAR
B. VARCHAR
C. BINARY
D. BIT -- B

5. Which type stores binary strings rather than normal character strings?
A. VARCHAR
B. TEXT
C. VARBINARY
D. ENUM  -- C

6. You need to store a customer's date of birth: 1999-11-27
Which type is most appropriate?
A. YEAR
B. DATE
C. DATETIME
D. TIME  -- B

7. You need to store: 2026-09-23 10:30:45
Which is suitable?
A. DATE
B. YEAR
C. DATETIME
D. TIME  -- C

8. Which type is specifically available for storing JSON documents?
A. TEXT only
B. JSON
C. BLOB only
D. VARCHAR only  -- B

9. Which constraint uniquely identifies each row in a table?
A. CHECK
B. FOREIGN KEY
C. PRIMARY KEY
D. DEFAULT    -- C

10. Which statement is correct?
A. A table can have unlimited primary keys
B. A table can have only one primary key
C. A primary key can contain duplicate values
D. A primary key can normally contain NULL values  -- B

11. What does a foreign key primarily help maintain?
A. Sorting
B. Referential integrity
C. Data formatting
D. String length   -- B

12. Which constraint prevents a column from containing NULL?
A. UNIQUE
B. DEFAULT
C. NOT NULL
D. CHECK   -- C

13. Which constraint is used to enforce a business condition such as: salary > 0
A. CHECK
B. DEFAULT
C. PRIMARY KEY
D. FOREIGN KEY   -- A

14. Which command changes the structure of an existing table?
A. UPDATE
B. ALTER
C. INSERT
D. SELECT   -- B

15. Which command completely removes a table definition?
A. DELETE
B. TRUNCATE
C. DROP
D. REMOVE  -- C

16. Which command removes rows while keeping the table structure?
A. DROP
B. TRUNCATE
C. ALTER
D. RENAME   -- B

17. Which command is used to add new records?
A. INSERT
B. UPDATE
C. ALTER
D. CREATE   -- A

18. Which command changes existing row data?
A. ALTER
B. UPDATE
C. CREATE
D. RENAME   -- B

19. Which command is transaction control?
A. COMMIT
B. CREATE
C. INSERT
D. ALTER   -- A

20. Which of these best represents a composite key?
A. One column with PRIMARY KEY
B. Two or more columns together forming a key
C. A foreign key only
D. A nullable column with UNIQUE  -- B

SECTION B — FILL IN THE BLANKS
21. The SQL data type used for exact fixed-point numerical values is ___DECIMAL_______
22. ____VARCHAR______ is generally used when the string length can vary.
23. A column that uniquely identifies every row can be defined as a ____PRIMARY______ key
24. A key that references a key in another table is called a ___FOREIGN_______ key
25. The constraint that prevents NULL values is ____NOT NULL______
26. The constraint that supplies a value automatically when one isn't provided is __DEFAULT________
27. The command used to add a new column to an existing table is _____ALTER TABLE_____
28. The command used to permanently remove a table and its definition is ___DROP TABLE_______
29. The command used to permanently save the current transaction is ____COMMIT______
30. A key consisting of multiple columns is called a ____COMPOSITE______ key

SECTION C — TRUE OR FALSE
Write TRUE / FALSE and correct the statement if it is false
31. DECIMAL is generally preferred over floating-point types when exact decimal values are important
TRUE — DECIMAL is suitable when exact decimal precision matters

32. A table can have multiple primary keys
FALSE — A table can have only one primary key constraint, although it may contain multiple columns (composite PK)

33. A foreign key must always be the primary key of the child table
FALSE — A foreign key does not have to be the primary key of the child table

34. A foreign key can reference a key in another table
TRUE — A foreign key references a key in another table

35. DELETE and DROP perform the same operation
FALSE — DELETE removes rows; DROP removes the table definition and data

36. TRUNCATE removes the table structure along with the data
FALSE — TRUNCATE removes rows but retains the table structure

37. VARCHAR and CHAR are identical in how they store character strings
FALSE — CHAR is fixed-length; VARCHAR is variable-length

38. NOT NULL guarantees uniqueness
FALSE — NOT NULL prevents NULL, but does not guarantee uniqueness

39. A CHECK constraint can enforce conditions on column values
TRUE — CHECK can enforce conditions such as salary > 0

40. COMMIT is used to permanently save transaction changes
TRUE — COMMIT saves transaction changes

SECTION D — MATCH THE FOLLOWING
Match Column A with Column B
 Column A                Column B                    
 41. BIGINT     C    | A. Prevent NULL             
 42. DECIMAL    D    | B. Remove table definition  
 43. JSON       F    | C. Large integer            
 44. VARCHAR    E    | D. Exact decimal number     
 45. PRIMARY KEY I   | E. Variable-length text     
 46. FOREIGN KEY G   | F. JSON document            
 47. NOT NULL    A   | G. Reference another table  
 48. ALTER       H   | H. Modify structure         
 49. DROP        B   | I. Uniquely identify a row  
 50. ROLLBACK    J   | J. Undo transaction changes 


SECTION E — IDENTIFY THE PROBLEM(Don't just say wrong - Explain why)
51. A developer creates:
CREATE TABLE products (
    product_id INT,
    price VARCHAR(20)
);
The application needs to calculate: price × quantity
What is the design problem?
What data type would you choose for price?

Problem: Price is stored as text, making numeric calculations inappropriate/awkward
Better: price DECIMAL(10,2)

52. A developer creates:
CREATE TABLE customers (
    customer_id INT PRIMARY KEY,
    email VARCHAR(150)
);
The business requirement says: Every customer must have an email and two customers cannot share the same email
What is missing?

Need: email VARCHAR(150) NOT NULL UNIQUE
Why:
NOT NULL -> mandatory
UNIQUE -> no duplicate emails

53. A developer creates:
CREATE TABLE orders (
    order_id INT PRIMARY KEY,
    customer_id INT
);
And there is already: customers(customer_id)
What constraint would you add to maintain the relationship?

Add:
FOREIGN KEY (customer_id)
REFERENCES customers(customer_id)

54. A developer executes:
DROP TABLE orders;
They intended only to remove today's order records
What did they do wrong?
Which command/approach better matches the requirement?

DROP TABLE orders; removes the entire table, not just records
For removing all rows while keeping structure: TRUNCATE TABLE orders;

55. A developer chooses:
mobile_number INT
for customer phone numbers
Why could this be a poor schema design?
Give a better choice and explain your reasoning

INT is a poor choice for phone numbers because phone numbers may contain:
Leading zeros
+
Country codes
Formatting characters

Better: phone_number VARCHAR(20)

SECTION F — COMMAND DECISION TEST(Here you are not given the command. You must identify it)
56. A company wants to create a new delivery_partners table
What command?

Create a table: CREATE TABLE delivery_partners (...);

57. A company wants to add: to an existing orders table
What command?

Add a column:
ALTER TABLE orders
ADD delivery_address VARCHAR(255);

58. A company wants to rename:
to: What command?

Rename table: RENAME TABLE old_products TO products;

59. A developer inserted incorrect data and the transaction has not yet been committed.
What command can undo the transaction changes?

Undo uncommitted transaction: ROLLBACK;

60. Management wants the table to remain available, but all current records must be removed
Which command would you consider?
Explain why you didn't choose DROP

SECTION G — REAL-TIME DATA TYPE DESIGN
You're designing a quick-commerce application
Choose an appropriate data type for every field and explain why

Remove all records but keep the table: TRUNCATE TABLE table_name;
Why not DROP?
DROP removes the table itself

61. customer_id
Requirement: Unique numeric identifier that may grow to very large values.

62. product_price
Requirement: Values such as 49.99, 199.50, 1299.95.

63. customer_name
Requirement: Names have different lengths

64. order_created_at
Requirement: Store date and time of an order

65. delivery_location
Requirement: Store geographic coordinates/location information
Which MySQL type/family would you investigate?


A. I didn't know the concept
B. I knew the concept but chose the wrong data type
C. I knew the concept but misunderstood the business requirement
D. I knew the answer but made a syntax mistake
E. I got the answer but cannot explain WHY

65-Question Final Revision Cheat Sheet chuskondii
INT          -> Normal integers
BIGINT       -> Very large integers
DECIMAL      -> Exact numeric / money
FLOAT/DOUBLE -> Approximate numeric
CHAR         -> Fixed-length text
VARCHAR      -> Variable-length text
TEXT         -> Large text
BINARY       -> Fixed-length binary
VARBINARY    -> Variable-length binary
BLOB         -> Binary data
DATE         -> Date only
TIME         -> Time only
DATETIME     -> Date + time
TIMESTAMP    -> Date/time with timestamp semantics
YEAR         -> Year
JSON         -> JSON documents
POINT        -> Spatial coordinate

PRIMARY KEY  -> Unique row identity
FOREIGN KEY  -> Relationship between tables
UNIQUE       -> Prevent duplicate values
NOT NULL     -> Prevent NULL
DEFAULT      -> Automatic default value
CHECK        -> Enforce condition

CREATE       -> Create object
ALTER        -> Change structure
DROP         -> Remove object
TRUNCATE     -> Remove all rows, keep structure
RENAME       -> Rename object
INSERT       -> Add rows
UPDATE       -> Modify rows
DELETE       -> Remove rows
SELECT       -> Read data

COMMIT       -> Save transaction
ROLLBACK     -> Undo transaction
SAVEPOINT    -> Create rollback point
